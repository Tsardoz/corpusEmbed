# Indexing Report — corpusEmbed

Date: 2026-07-16

## Summary
Seven system-identification papers were added to the corpus and one paper (the
IEEE published "Road Map") was replaced with its cleaner preprint. The corpus was
re-embedded with SPECTER2 (+ proximity adapter) and re-scored against the six
bucket centroids. The database passes SQLite and domain integrity checks.

Net corpus change: 106 -> 113 papers.

## Corpus state
- Rows in `data/review.db`: 113
- DOIs: 106 with a DOI, 7 without (genuine grey literature / non-CrossRef items)
- `full_text_pagemarked`: 113/113 populated; `needs_ocr` = 0
- `abstract_fidelity_failed` = 1 on 10 rows — all pre-existing corpus papers, known
  false positives (two-column layout / metadata-page artifacts); none are the newly
  added papers.

## Bucket distribution (SPECTER2 cosine, argmax)
- crop_water_stress_physiology: 41
- ai_scheduling: 25
- remote_sensing_kc_et: 25
- leakage_methodology: 10
- system_identification: 7
- methods_background: 5
- unclassified: 0
- Total: 113

## system_identification members (7)
All seven system-identification papers are assigned to the bucket:
- 0.9347 Nonlinear System Identification: A User-Oriented Roadmap (preprint)
- 0.9146 Discovering governing equations from data by sparse identification (SINDy)
- 0.9086 Optimal Experiment Design for Open and Closed-Loop System Identification
- 0.9058 Nonlinear Black-box Modeling in System Identification: a Unified Overview
- 0.9032 System identification of nonlinear state-space models
- 0.9024 Data Informativity: A New Perspective on Data-Driven Analysis and Control
- 0.8992 Simulation error minimization identification based on multi-stage prediction

## Near-member review
Papers whose second-highest bucket is `system_identification`. Assignment is a
deterministic argmax over the six centroid cosines; the full score vector is stored
in `papers.scores_json` for downstream multi-label use. No manual override exists in
the pipeline.
- Bayesian P-Splines -> methods_background (margin 0.058). Correct: penalized-spline /
  Bayesian smoothing methodology, explicitly in scope for methods_background. Keep.
- Tutorial on Bayesian Functional Regression Using Stan -> methods_background
  (margin 0.043). Correct: functional-data methodology. Keep. (Was "unassigned" under
  the older taxonomy; now has a sensible home.)
- Variable hydraulic resistances and their impact on plant drought response modelling
  -> crop_water_stress_physiology (margin 0.047). Defensible but genuinely dual:
  mechanistic plant-hydraulic modelling (capacitance/resistance) is also explicitly in
  the system_identification scope. Recommended to treat as system_identification-secondary;
  it is the Baert 2015 paper central to the trunk-water / Windkessel approach.
- Profit Prediction Using ARIMA, SARIMA and LSTM Models -> ai_scheduling
  (margin 0.0045). Weak: a near three-way tie
  (ai_scheduling 0.8718 / system_identification 0.8672 / methods_background 0.8663).
  Finance-domain time-series forecasting, arguably out of scope for an irrigation
  corpus; the winning label is not meaningful. Recommend deciding whether it belongs
  in the corpus.

## DOI recovery
CrossRef bibliographic search (`recover_dois_review.py`) over the no-DOI rows:
- Written: `Nonlinear System Identification: A User-Oriented Roadmap` ->
  10.1109/mcs.2019.2938121 (candidate title "...Road Map", token-Jaccard 0.667;
  accepted manually as an unambiguous match).
- Auto-accepted (similarity 1.000): Optimal Experiment Design -> 10.4310/cis.2011.v11.n3.a1;
  Nonlinear Black-box Modeling -> 10.1016/0005-1098(95)00120-8.
- Left null: 7 rows, genuine grey literature / a mismatched candidate (the Goodwin
  "Scheduling deficit irrigation" candidate was a different apple-tree paper). See
  `export/doi_recovery_review.csv`.

## Full text in pass 1
Pass 1 now persists `full_text_pagemarked` (pdftotext -raw, `[PAGE n]` markers) and
`page_numbering` at extraction time, so downstream passes reuse stored text instead of
re-running pdftotext. Existing rows were backfilled (text-only, no model calls).

## Integrity
- `PRAGMA integrity_check` = ok; `PRAGMA foreign_key_check` = none
- No null/empty in required NOT NULL fields (filename, provenance, extraction_model,
  extraction_date)
- No duplicate filenames; no duplicate DOIs
- `embed.inputs.load_papers()` loads all 113 with no dedup-key assertion
- `embeddings.db`: 113 rows, all `embedding_model='specter2'`, `embedding_dim=768`,
  768-float32 vectors; six centroids; four write-time asserts passed

## Artifacts
Regenerated by `python -m embed.build` (gitignored):
- `data/embeddings.db`
- `export/coverage.csv`, `export/vectors.npy`, `export/keys.txt`, `export/report.txt`
- `export/doi_recovery_review.csv`

## Open items
- Decide whether the ARIMA/SARIMA/LSTM profit-forecasting paper belongs in the corpus.
- Optionally treat "Variable hydraulic resistances" (Baert 2015) as a
  system_identification paper for review purposes.
- The 7 DOI-less rows are believed to be genuinely without a DOI; no action needed
  unless a source is found.
