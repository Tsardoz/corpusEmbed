# Python 3.12 venv (.venv). Install torch from the CPU wheel index:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# SPECTER2 (allenai/specter2_base + proximity adapter) is loaded from the local
# HF cache; no download is needed at build time.
torch==2.13.0
transformers==4.57.6
adapters==1.3.0
numpy==2.5.1
pyyaml==6.0.3
